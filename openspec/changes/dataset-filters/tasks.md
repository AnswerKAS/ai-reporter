# Tasks: dataset-filters

## 1. Примитив множественного выбора

- [x] 1.1 `components/ui/Chips.tsx` — группа фильтров: `values`/`onToggle`, счётчик у каждого варианта, `aria-pressed`, вариант с нулём отключён
- [x] 1.2 `components/ui/index.ts` — экспорт `Chips` и `ChipOption`

## 2. Страница датасетов

- [x] 2.1 `pages/DatasetsPage.tsx` — полоса фильтров над плиткой: поиск, источники, статусы, счётчик «показано N из M», сброс
- [x] 2.2 Отбор: поиск по названию, slug'у, описанию, таблице и именам полей; источники и статусы — по «или», пустой набор не фильтрует
- [x] 2.3 Счётчики вариантов считаются с учётом остальных фильтров — число у варианта равно тому, что покажет нажатие
- [x] 2.4 Фильтры в адресе (`?source=`, `?status=`), `replace` вместо новой записи истории на каждое нажатие; набор держится состоянием, адрес ему зеркалится — считать новый набор от адреса нельзя, роутер обновляет его переходом и второе из двух быстрых нажатий затирало первое
- [x] 2.5 Пустой результат — `EmptyState` со сбросом; выпавший из фильтра выбранный датасет закрывается

## 3. Документация

- [x] 3.1 `AGENTS.md` — фильтры страницы датасетов и примитив `Chips`
- [x] 3.2 `README.md` — фильтры в описании страницы «Датасеты»
- [x] 3.3 Проверка: `npm run lint && npm run build`, `openspec validate --all`
