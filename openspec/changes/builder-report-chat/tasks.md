# Tasks: builder-report-chat

## 1. Диалог (бэкенд)

- [x] 1.1 `query/interpret.py` — модели и пределы диалога: `CHAT_MODEL` (`deepseek/deepseek-v4-flash`), `CHAT_FALLBACK_MODEL` (`z-ai/glm-5.3-flash`), `CHAT_TIMEOUT` (60 с); замер в комментарии переписан по свежему прогону
- [x] 1.2 `query/interpret.py` — `_ask` принимает предел ожидания параметром: у диалога он свой
- [x] 1.3 `query/interpret.py` — `_CHAT_INSTRUCTIONS`: формат ответа `{reply, title, definition}`, подробная раскладка (итоги, динамика, разбивки, таблица), фильтр по каждому использованному разрезу, правка текущего определения целиком, правила словаря и арифметики — общие с одиночным разбором
- [x] 1.4 `query/interpret.py` — `chat(messages, catalog, ...)`: сборка промпта из переписки и текущего определения, разбор ответа, сверка `_validate`; ошибка сверки возвращается репликой, а не исключением
- [x] 1.5 `query/interpret.py` — предел длины переписки (последние реплики), пустая переписка и пустой словарь отклоняются `DatasetError`

## 2. API

- [x] 2.1 `api/reports.py` — `POST /api/reports/chat`: переписка, поля, формулы, датасеты и текущее определение на вход; `reply`, `definition`, `title`, `notes`, `source`, `model` на выход
- [x] 2.2 `tests/test_routes.py` — метод в описи

## 3. Интерфейс

- [x] 3.1 `lib/api.ts` — `chatReport(messages, context)`; типы `ChatMessage`, `ChatAnswer`
- [x] 3.2 `components/builder/ReportChat.tsx` — переписка, поле ввода, ожидание ответа, ошибка; вторая реплика не принимается, пока идёт ответ
- [x] 3.3 `components/builder/ReportChat.tsx` — подтверждение: «Сохранить отчёт» (админу — сохраняет и уводит на отчёт, остальным — объяснение), «Вернуть как было», правка предложенного названия
- [x] 3.4 `pages/BuilderPage.tsx` — блок «опишите словами» заменён диалогом; применение предложения переиспользует разбор (датасеты, показатели, разрезы, формулы), прежняя раскладка запоминается для отката

## 4. Тесты и проверка

- [x] 4.1 `tests/test_interpret_chat.py` — фейковая модель: правка определения целиком, ошибка словаря репликой, уточняющий вопрос без определения, обрыв модели, обрезка переписки
- [x] 4.2 `tests/test_api_reports.py` — метод диалога: 401 без токена, 422 на пустой переписке и пустом словаре, обычный ответ
- [x] 4.3 `cd backend && .venv/bin/python -m pytest`
- [x] 4.4 `npm run build` и `npm run lint`
- [x] 4.5 Проверка в браузере (временная страница с заглушкой API, удалена после): диалог, правка вторым ходом, откат, ошибка словаря репликой, предпросмотр на данных, подтверждение админом; плюс живой прогон `interpret.chat` на deepseek — 6 секций и 4 фильтра на «покажи продажи максимально подробно», правка вторым ходом за 3.5 с
- [x] 4.6 `AGENTS.md`, `.env` с новыми переменными и `openspec validate builder-report-chat --strict`

## 5. Найдено по ходу

- [x] 5.1 `interpret._filters` — вид фильтра по типу разреза: модель писала `select` по разрезу-дате, и читатель получал список всех дат вместо периода. Чинит и одиночный разбор
- [x] 5.2 Диалог перенесён над шагами: на шаге «Раскладка» он был заперт за выбором первого показателя
- [x] 5.3 `CHAT_TIMEOUT` = 90 с: живой первый ход на 139 показателях занял 42 с, 60 с оставляли мало запаса
- [x] 5.4 Собеседник больше не отказывается: словарь диалога полный (выбранные датасеты — предпочтение), инструкция запрещает отказ и требует брать ближайшее по смыслу, ошибка сверки переспрашивается у модели и только повторный промах идёт репликой
- [x] 5.5 Диалог переехал в модалку: на странице — кнопка и полоса подтверждения, переписка переживает закрытие окна
