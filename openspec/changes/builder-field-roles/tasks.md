# Tasks: builder-field-roles

## 1. Роли полей на раскладке

- [x] 1.1 `pages/BuilderPage.tsx` — `PaletteLabel`: подпись вида полей со счётчиком и ссылкой на невыбранное
- [x] 1.2 Палитра считает невыбранные поля датасета (`unpickedMetrics`, `unpickedDimensions`) и поясняет разницу показателя и разреза
- [x] 1.3 Пустой блок «Фильтры отчёта» объясняет, что фильтр строится по разрезу
- [x] 1.4 Карточка KPI объясняет, почему разрезы к ней не применяются; у графика и таблицы блок называется «Разрезы (группировка)»

## 2. Понятные ошибки разбора описания

- [x] 2.1 `query/interpret.py` — промпт просит описать нехватку своими словами и назвать слова пользователя (`missing`), а не возвращать заготовку
- [x] 2.2 `_missing_message`, `_known`, `_where_to_add` — ошибка называет слово, перечисляет доступное и говорит, где завести поле
- [x] 2.3 Пустое описание, ответ без секций, выдуманные имена, таймаут и проза модели — тексты переписаны под действие
- [x] 2.4 Отчёт о разборе моделью называет взятые показатели и разрезы
- [x] 2.5 `lib/api.ts` + конструктор — причина отката к парсеру видна человеку
- [x] 2.6 `vocabulary_of(..., datasets)` — словарь сужается до выбранных датасетов; конструктор передаёт их в `parsePhrase`
- [x] 2.7 `_computed()` — формула из двух показателей словаря: операнды и действие проверяются, ключ не затеняет показатель, секции переписываются
- [x] 2.8 Промпт: правило арифметики стоит до правила «верни ошибку» и подкреплено разобранным примером — без него модель принимала «A разделить на B» за название показателя
- [x] 2.9 Ошибка не называет ненайденным то, что в словаре есть
- [x] 2.10 `pages/BuilderPage.tsx` — формулы разбора попадают в набор отчёта
- [x] 2.11 `unsupported` в ответе модели — предел формата (умножение на число, три операнда) объясняется отдельно от нехватки поля
- [x] 2.12 `components/builder/VocabularyDialog.tsx` + кнопка «?» у поля описания — словарь отчёта с поиском

## 3. Значения источника

- [x] 3.1 `reports/executor.py` и `reports/drilldown.py` — байты из источника декодируются в текст

## 4. Тесты и документация

- [x] 4.1 `tests/test_interpret.py` — сообщения об ошибках проверяются по содержанию, а не по факту исключения
- [x] 4.2 `tests/test_executor.py`, `tests/test_drilldown.py` — байты приводятся к тексту
- [x] 4.3 `AGENTS.md` — роли полей на раскладке и правило текстов ошибок разбора
- [x] 4.4 Проверка: `pytest`, `npm run lint && npm run build`, `openspec validate --all`
