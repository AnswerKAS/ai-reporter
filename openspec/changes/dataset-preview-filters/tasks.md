# Tasks: dataset-preview-filters

- [x] 1.1 `components/DatasetPreviewTable.tsx` — таблица превью с сортировкой по заголовку (по кругу: ↑ / ↓ / без) и строкой фильтров под заголовками
- [x] 1.2 Сравнение значений — числовое для чисел, словарное для текста (`Intl.Collator` с `numeric`)
- [x] 1.3 Счётчик «строк: N из M», сброс фильтров, объяснение при пустом результате
- [x] 1.4 `aria-sort` на заголовке и подпись у каждого поля фильтра
- [x] 1.5 `pages/DatasetsPage.tsx` — инлайновая таблица превью заменяется компонентом
- [x] 1.6 Проверка: `npm run lint && npm run build`, разбор в браузере, `openspec validate --all`
