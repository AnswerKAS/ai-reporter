# Tasks: builder-dataset-picker

## 1. Группы связей

- [x] 1.1 `lib/dataset-links.ts` — `buildLinkIndex(links)`: список смежности, компоненты связности обходом в ширину (`clusterOf: Map<slug, number>`, `clusterSize: Map<number, number>`), `neighbours(slug)`, `reachableFrom(slugs)`
- [x] 1.2 `styles/theme.css` — токены `--cluster-1…--cluster-8` (рамка) и `--cluster-1-soft…` (заливка) в `:root` и `.dark`, наружу через `@theme inline`
- [x] 1.3 `lib/dataset-links.ts` — `clusterTone(index)`: классы Tailwind по номеру группы, циклом по палитре; номер группы отдаётся подписью, а не только цветом

## 2. Карточка и плитка

- [x] 2.1 `components/builder/DatasetPicker.tsx` — карточка: название, slug, статус (`Badge`), источник и таблица либо «SQL-запрос», число полей схемы, показателей и разрезов словаря, число связей и группа, пометка «поля заводятся вручную», у выбранного — сколько полей отмечено; `aria-pressed`; крестик «убрать» отдельной кнопкой
- [x] 2.2 `components/builder/DatasetPicker.tsx` — полоса отбора: поиск (название, slug, описание, таблица, имена полей), `Chips` по источникам и статусам со счётчиками, быстрые отборы «выбранные» / «связанные с выбранным» / «со словарём», счётчик «показано N из M» и сброс
- [x] 2.3 `components/builder/DatasetPicker.tsx` — порядок: выбранные, связанные с выбранными, дальше по алфавиту (`Intl.Collator('ru')`); порция 60 карточек, кнопка «Показать ещё», сброс порции при смене отбора
- [x] 2.4 `components/builder/DatasetPicker.tsx` — строка выбранных над плиткой: чипы со снятием, видны независимо от отбора
- [x] 2.5 `components/builder/DatasetPicker.tsx` — поисковый индекс `slug → строка` считается один раз на список датасетов

## 3. Снятие датасета

- [x] 3.1 `pages/BuilderPage.tsx` — `removeDataset(slug)` в `Builder`: уходят словарные показатели и разрезы датасета, свои поля, формулы на них, ссылки в секциях (`metrics`, `by`, `orderBy`, `grain`) и фильтры по его разрезам
- [x] 3.2 `pages/BuilderPage.tsx` — подтверждение через `useConfirm`, когда снятие задевает секции: называется число полей и секций; иначе снятие проходит молча
- [x] 3.3 `pages/BuilderPage.tsx` — `StepData` получает `onRemoveDataset` и `onToggleDataset` готовыми действиями; собственная `toggleDataset` уходит
- [x] 3.4 `pages/BuilderPage.tsx` — шапка раскрытой секции датасета: счётчик отмеченных полей и крестик «убрать датасет»

## 4. Проверка

- [x] 4.1 `npm run build` (tsc + vite) и `npm run lint` без ошибок
- [x] 4.2 Проверка в браузере на 1200 датасетах (временная страница с заглушкой API, удалена после проверки): выбор и снятие датасета с подтверждением, цвета групп в светлой и тёмной теме, поиск по именам колонок, отбор «связанные с выбранным», порции по 60, поиск по полям датасета
- [x] 4.3 `AGENTS.md` — раздел о конструкторе дополнен правилами шага «Данные»
- [x] 4.4 `openspec validate builder-dataset-picker --strict`
