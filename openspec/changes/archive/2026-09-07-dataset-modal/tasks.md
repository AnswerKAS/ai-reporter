# Tasks: dataset-modal

- [x] 1.1 `components/ui/Modal.tsx` — размер `xl` (шире и выше, под таблицы)
- [x] 1.2 `components/DatasetModal.tsx` — окно вкладками: «Превью», «Поля датасета» (только описание полей), «Показатели», «Разрезы», «Черновик словаря» и «Источник» (обе — админу); действия администратора в подвале
- [x] 1.3 Сопоставление словаря полям: разрез — по `field`, показатель — по упоминанию колонки в выражении по границе слова; у показателя перечисляются все его колонки (пусто у `count()`, две у `sum(a - b)`), статус `error` виден; разрез на поле, которого нет в схеме, помечен
- [x] 1.4 Редактор SQL-запроса переезжает из `pages/DatasetsPage.tsx` в окно
- [x] 1.5 `pages/DatasetsPage.tsx` — карточка открывает окно, панель под плиткой убрана
- [x] 1.6 `components/admin/DatasetsPanel.tsx` — строка открывает то же окно, добавлено подтверждение удаления
- [x] 1.7 Загрузка CSV показывается только у CSV-датасета (остальным она отвечала 409)
- [x] 1.8 Проверка: `npm run lint && npm run build`, окно в браузере на обоих экранах, `openspec validate --all`
