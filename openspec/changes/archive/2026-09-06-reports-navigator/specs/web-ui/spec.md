# web-ui — веб-интерфейс

## MODIFIED Requirements

### Requirement: Левое меню
Авторизованному пользователю SHALL показываться левое меню с навигатором по
каталогу отчётов. Меню SHALL находить отчёт среди десятков тысяч: поиск уходит
на сервер с задержкой, выдача приходит страницами и догружается по мере
прокрутки — весь список на клиент не выгружается.

Меню SHALL показывать сверху закреплённые и недавно открытые отчёты, полосу
фильтров со счётчиками (избранное, группа доступа, автор, тема, статус) и
переключатель «Группировать по» (без группировки, по группе доступа, по автору,
по теме). При группировке SHALL показываться заголовки групп со счётчиками, а
строки группы догружаться при её раскрытии. Выбранные фильтры и группировка
SHALL переживать перезагрузку страницы.

На строке отчёта SHALL оставаться действия — открыть в конструкторе,
переименовать, удалить (по правам); те же действия SHALL быть доступны без
наведения мыши.

Ширину меню SHALL можно менять перетаскиванием разделителя (200–560 px) и с
клавиатуры (стрелки, Home), выбранное значение сохраняется между сессиями. На
узком экране меню сворачивается в мобильное.

#### Scenario: Изменённая ширина меню
- **WHEN** читатель растянул левое меню и вернулся на сайт позже
- **THEN** меню открывается прежней ширины

#### Scenario: Поиск среди десяти тысяч
- **WHEN** читатель набирает в поиске три буквы
- **THEN** запрос уходит на сервер один раз после паузы, а в меню встаёт первая страница совпадений с числом найденных

#### Scenario: Раскрытие группы
- **WHEN** читатель выбрал группировку по группе доступа и раскрыл группу на три тысячи отчётов
- **THEN** подгружается первая страница этой группы, остальные — по мере прокрутки

#### Scenario: Возврат к своим отчётам
- **WHEN** читатель закрепил пять отчётов
- **THEN** они стоят в отдельном блоке наверху меню, независимо от поиска и фильтров

#### Scenario: Разрезы каталога недоступны
- **WHEN** запрос за фасетами не удался (например, сервер их ещё не отдаёт)
- **THEN** меню всё равно показывает список отчётов, а сообщение объясняет, что недоступны именно фильтры и группировка

#### Scenario: Действия без мыши
- **WHEN** отчёт открывают с тач-устройства или с клавиатуры
- **THEN** переименование, конструктор и удаление доступны без наведения курсора

### Requirement: Единый список отчётов
Каталог отчётов SHALL жить в одном контексте приложения: фасеты, избранное и
недавние — общие для всех экранов. После создания, правки или удаления отчёта
каталог SHALL перечитываться, чтобы меню и страницы показывали одно и то же.

#### Scenario: Отчёт создан в конструкторе
- **WHEN** автор сохранил новый отчёт-конструктор
- **THEN** отчёт появляется в меню слева без перезагрузки страницы

#### Scenario: Отчёт закреплён на одной странице
- **WHEN** читатель закрепил отчёт на странице списка
- **THEN** он сразу появляется в закреплённых в меню слева
