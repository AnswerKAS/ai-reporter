name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

env:
  APP_DIR: /home/deploy/ai-reporter
  SERVICE_NAME: ai-reporter

jobs:
  build:
    name: Сборка и проверки
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - uses: actions/setup-python@v5
        with:
          # Ровно та версия, что стоит на VPS: на 3.12 проходит синтаксис,
          # который системный python3.10 сервера не парсит, и сборка тогда
          # пропускает код, падающий на импорте уже в проде.
          python-version: '3.10'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: "Frontend: lint + build"
        working-directory: frontend
        run: |
          npm ci
          npm run lint
          npm run build

      - name: "Backend: зависимости + smoke-импорт"
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -c "import sys; sys.path.insert(0, '.'); import app.main; print('backend import ok')"

      - name: Загрузить frontend/dist
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/
          retention-days: 7

  deploy:
    name: Деплой на VPS
    needs: build
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/

      - name: Настроить SSH (ключ + known_hosts)
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SSH_KNOWN_HOSTS: ${{ secrets.SSH_KNOWN_HOSTS }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
        run: |
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          printf '%s\n' "$SSH_PRIVATE_KEY" > ~/.ssh/id_deploy
          chmod 600 ~/.ssh/id_deploy
          printf '%s\n' "$SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts
          # параметры в config, а не в командной строке: rsync передаёт строку
          # --rsh в exec без раскрытия ~, а ssh свой config читает сам
          {
            printf 'Host %s\n' "$SSH_HOST"
            printf '  User %s\n' "$SSH_USER"
            printf '  IdentityFile ~/.ssh/id_deploy\n'
            printf '  IdentitiesOnly yes\n'
            printf '  BatchMode yes\n'
            printf '  StrictHostKeyChecking yes\n'
          } > ~/.ssh/config
          chmod 600 ~/.ssh/config

      - name: Проверка SSH-доступа
        env:
          SERVER: ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}
        run: |
          ssh -v -o ConnectTimeout=15 \
            $SERVER 'echo SSH-OK' 2>&1 | tee /tmp/ssh-check.log | tail -5
          grep -q "SSH-OK" /tmp/ssh-check.log && echo "✓ SSH-подключение работает" || {
            echo "::error::SSH не подключился — см. /tmp/ssh-check.log ниже"
            grep -E "Authentications that|Permission denied|Host key verification|Connection (timed out|refused|reset)|invalid user|docker: " /tmp/ssh-check.log || true
            exit 1
          }

      - name: Синхронизация кода (rsync)
        env:
          SERVER: ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}
        run: |
          RSH="ssh"
          rsync -az --delete --rsh="$RSH" backend/app/      $SERVER:$APP_DIR/backend/app/
          rsync -az           --rsh="$RSH" backend/requirements.txt $SERVER:$APP_DIR/backend/
          rsync -az --delete --rsh="$RSH" frontend/dist/    $SERVER:$APP_DIR/frontend/dist/
          rsync -az --delete --rsh="$RSH" deploy/           $SERVER:$APP_DIR/deploy/

      - name: Установка зависимостей и перезапуск сервиса
        env:
          SERVER: ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}
        run: |
          ssh $SERVER \
            "cd $APP_DIR/backend && .venv/bin/pip install -q -r requirements.txt && sudo systemctl restart $SERVICE_NAME"

      - name: Проверка живости бэкенда
        env:
          SERVER: ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}
        run: |
          # systemctl is-active для Type=simple отвечает "active" сразу после
          # запуска процесса — ещё до импорта приложения, поэтому падение старта
          # он не ловит. Живость подтверждает только ответ самого бэкенда.
          # Опрашиваем интерпретатором из venv, а не curl: этот бинарник на
          # сервере есть заведомо — им же запускается сам сервис.
          ssh $SERVER "
            cd $APP_DIR/backend
            for i in \$(seq 1 15); do
              if .venv/bin/python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health', timeout=5)\"; then
                echo '✓ бэкенд отвечает на /api/health'
                exit 0
              fi
              sleep 2
            done
            echo '::error::бэкенд не ответил на /api/health за 30 секунд'
            systemctl status $SERVICE_NAME --no-pager || true
            journalctl -u $SERVICE_NAME -n 50 --no-pager || true
            exit 1
          "
